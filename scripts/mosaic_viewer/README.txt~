To launch:

- edit mosaic.conf rtsp addresses according to current IP camera address
- make sure fake.png and mosaic.conf are in the same folder.
- open a terminal, cd to that folder
- run the command in 'terminal command'
